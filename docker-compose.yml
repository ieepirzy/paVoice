services:
  tts:
    build: .
    devices:
      - /dev/snd:/dev/snd
    environment:
      - ALSA_CARD=1
      - ALSA_DEVICE=plughw:1,0
    ports:
      - "8765:8765"
    restart: unless-stopped

    networks:
      - agent-net

networks:
  agent-net:
    driver: bridge